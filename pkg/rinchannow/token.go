package rinchannow

const (
	next      = 'リ' // ポインタをインクリメント
	prev      = 'ン' // ポインタをデクリメント
	incr      = 'ち' // メモリの値をインクリメント
	decr      = 'ゃ' // メモリの値をデクリメント
	putchar   = 'ん' // メモリの値を出力
	getchar   = 'な' // 入力をメモリに格納
	loopstart = 'う' // メモリの値が0でない場合、対応する「！」までジャンプ
	loopend   = '！' // メモリの値が0でない場合、対応する「う」までジャンプ
)
